import { StockFilerPipe } from './stock-filer.pipe';

describe('StockFilerPipe', () => {
  it('create an instance', () => {
    const pipe = new StockFilerPipe();
    expect(pipe).toBeTruthy();
  });
});
